version: "3"

services:
  mc:
    image: itzg/minecraft-server:java8
    ports:
      - 25565:25565
    environment:
      EULA: "TRUE"
      TYPE: "FORGE"
    tty: true
    stdin_open: true
    restart: always
    volumes:
    - ${HOME}/minecraft:/data
